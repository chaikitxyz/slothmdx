---
import siteConfig from "src/siteConfig.json";
import { Icon } from "astro-icon";
---

<div class="flex flex-wrap items-end gap-x-4">
	<p>{siteConfig.socialLinks.title}</p>
	<ul class="flex flex-1 items-center gap-x-2 sm:flex-initial">
		{
			siteConfig.socialLinks.socialLinksList.map(({ link, name, friendlyName }) => (
				<li class="flex">
					<a
						class="inline-block p-1 sm:hover:text-link"
						href={link}
						target="_blank"
						rel={`noopener noreferrer`}
					>
						<Icon class="h-6 w-6" name={name} aria-hidden="true" focusable="false" />
						<span class="sr-only">{friendlyName}</span>
					</a>
				</li>
			))
		}
	</ul>
</div>
